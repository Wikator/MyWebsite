<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Blog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
    <link href="
      https://cdn.jsdelivr.net/npm/bootswatch@5.3.3/dist/journal/bootstrap.min.css"
          rel="stylesheet">
  </head>

  <body>
    <header data-bs-target="dark">
      <%= render 'layouts/navbar' %>
    </header>
    <main>
      <noscript>
        <% unless current_page?(home_index_path) || current_page?(root_path) %>
          <div class="noscript-warning">
            <div class="alert alert-danger" role="alert">
              You do not have JavaScript enabled. All of the site's functionality should work okay,
              except for deleting comments and reactions.
            </div>
          </div>
        <% end %>
      </noscript>
      <div id="flash">
        <%= render 'layouts/flash' %>
      </div>
      <%= yield %>
    </main>
  </body>
</html>
